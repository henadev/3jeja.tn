<div class="table-responsive mx-2">
    <?php if( $countcomment > 0 ) : ?>
          <small class="float-right m-5 text-primary">(<?= $countcomment; ?>) Comment</small>
          <small class="float-right m-5 text-primary">(<?= $countcomment; ?>) vu</small>
    <?php endif; ?>
        <table class="table table-striped table-sm">
          <thead>
            <tr>
              <th scope="col">Id</th>
              <th scope="col">Comment</th>
              <th scope="col" class="text-center" colspan="2">Action</th>
            </tr>
          </thead>
          <tbody>
          <?php foreach($comments as $comment) : ?>
            <tr>
              <td><?= $comment['id'] ?></td>
              <td><?= $comment['comment'] ?></td>
              <td>
                <button type="button" 
                  class="btn btn-danger" 
                  data-toggle="modal" 
                  data-target="#myModal<?= $comment['id'] ?>">
                  <i class="fa fa-trash" aria-hidden="true"></i>
                </button>
<!-- modal content -->
                  <div id="myModal<?= $comment['id'] ?>"  class="modal" tabindex="-1">
                    <div class="modal-dialog">
                      <div class="modal-content">
                        
                        <div class="modal-header">
                          <h5 class="modal-title">Modal title</h5>
                          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                          </button>
                        </div>

                        <div class="modal-body">
                          <p>Voulez vous supprimer le commentaire N°<?= $comment['id'] ?></p>
                        </div>

                        <div class="modal-footer">
                          <a class="btn btn-danger btn-lg "
                            data-indice="<?= intval( $comment['id'] ); ?>"
                            title="Supprimer le post"
                            href="admin_comment_delete.php?id=<?= $comment['id'] ?>">
                            <i class="fa fa-trash" aria-hidden="true"></i>
                          </a>
                          <a class="btn btn-dark btn-lg"
                            data-dismiss="modal"
                            title="J'abandonne !"
                            href="admin_author_index.php">
                            <i class="fa fa-hand-paper" aria-hidden="true"></i>
                          </a>
                        </div>

                      </div>
                    </div>
                </div>
              </td>
            </tr>
            <?php endforeach ; ?>
          </tbody>
        </table>
      </div>